import type { LabelMap } from "../labels";

/** English defaults for the display components. Keys are "<component>.<name>"; values are strings with {name} placeholders or functions. */
export const DISPLAY_LABELS = {} as const satisfies LabelMap;
