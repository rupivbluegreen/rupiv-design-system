import type { LabelMap } from "../labels";

/** English defaults for the data components. Keys are "<component>.<name>"; values are strings with {name} placeholders or functions. */
export const DATA_LABELS = {} as const satisfies LabelMap;
