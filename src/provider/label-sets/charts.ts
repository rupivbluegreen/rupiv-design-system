import type { LabelMap } from "../labels";

/** English defaults for the charts components. Keys are "<component>.<name>"; values are strings with {name} placeholders or functions. */
export const CHART_LABELS = {} as const satisfies LabelMap;
