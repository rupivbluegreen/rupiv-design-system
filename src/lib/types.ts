/** Semantic tone: what the person must do, not which module it came from. Maps to the `--{tone}-*` tokens. */
export type Tone = "neutral" | "accent" | "success" | "warning" | "danger" | "info";

/** Categorical colour for tags and avatars. Maps to the `--cat-{name}-bg` and `--cat-{name}-fg` tokens. */
export type CategoryColor = "indigo" | "madder" | "turmeric" | "neem" | "lac" | "kattha" | "slate";
